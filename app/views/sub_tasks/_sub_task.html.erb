<p>
	<ul>
		<li id = '<%= sub_task.id %>'><%= sub_task.content %>
			<small> 
				<%= link_to "remove", task_sub_task_path(@task, sub_task.id), 
					method: :delete, data: {confirm: "Are you sure?"}, remote: true%> 
			</small>
		</li>
	</ul>
	<p id = 'sub_task_id'></p>
</p>